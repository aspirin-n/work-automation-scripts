name: Universal to Google Drive

on:
  workflow_dispatch:
    inputs:
      url:
        description: 'Direct Download URL (Any website)'
        required: true
        type: string
      filename:
        description: 'What to save the file as (e.g., video.mp4, game.zip)'
        required: true
        type: string

jobs:
  transfer:
    runs-on: ubuntu-latest
    steps:
      - name: Install Tools
        run: |
          sudo apt-get update
          sudo apt-get install -y aria2
          # Install Rclone for Google Drive upload
          sudo -v ; curl https://rclone.org/install.sh | sudo bash

      - name: Setup Google Drive Auth
        env:
          # We pass the secret as an environment variable to prevent shell scrambling
          RCLONE_SECRET_DATA: ${{ secrets.RCLONE_CONFIG }}
        run: |
          mkdir -p ~/.config/rclone
          # Using single quotes and '$' keeps the text completely clean and raw
          echo "$RCLONE_SECRET_DATA" > ~/.config/rclone/rclone.conf

      - name: Download File
        env:
          URL: ${{ inputs.url }}
          FILENAME: ${{ inputs.filename }}
        run: |
          echo "Downloading file..."
          aria2c -x 16 -s 16 -o "$FILENAME" "$URL"
          
          echo "=== Download Complete ==="
          ls -lh "$FILENAME"

      - name: Upload to Google Drive
        env:
          FILENAME: ${{ inputs.filename }}
        run: |
          echo "Uploading to Google Drive..."
          rclone copy "$FILENAME" "gdrive:GitHub_Downloads/" -v --stats=5s
          echo "✅ Upload Complete! Check your Google Drive."
